machine:
  node:
    version: 4.3.1

dependencies:
    override:
        - npm install
        - npm install -g grunt-cli

test:
    override:
        - grunt sasslint
        - grunt lint:prod
        - grunt test
