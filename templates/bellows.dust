{?items}
    <div class="bellows">
        {#items}
            <div class="bellows__item {?isOpen}bellows--is-open{/isOpen}">
                <div class="bellows__header"><h3>{header}</h3></div>
                <div class="bellows__content"><p>{content}</p></div>
            </div>
        {/items}
    </div>
{/items}
